// function bgDecoEffect():Void
// {
//     var backDecoEffectLayer = tiledData.getLayer("mapDecoEffect");
//     var backDecoScrollBoost:Float = 0;
    
//     var scrollSettingsLayer = tiledData.getLayer("ScrollSettings");
//     if (scrollSettingsLayer != null && scrollSettingsLayer.type == TiledLayerType.OBJECT)
//     {
//         var castScroll:TiledObjectLayer = cast scrollSettingsLayer;
//         if (castScroll.properties.contains("speed"))
//             backDecoScrollBoost = Std.parseFloat(Std.string(castScroll.properties.get("speed")));
//     }

//     if (backDecoEffectLayer != null && backDecoEffectLayer.properties.contains("backDecoEffectType"))
//     {
//         var backDecoEffect = backDecoEffectLayer.properties.get("backDecoEffectType");

//         if (backDecoEffect != currentBackDecoEffectName || backDecoScrollBoost != lastDecoScrollBoost)
//         {
//             if (backDecoEffectObj != null) { remove(backDecoEffectObj); backDecoEffectObj.destroy(); backDecoEffectObj = null; }

//             switch (backDecoEffect)
//             {
//                 case "moon":
//                     backDecoEffectObj = new FlxBackdrop(AssetPaths.moon__png, X);
//                     backDecoEffectObj.velocity.set(-5, -3);
//                     backDecoEffectObj.alpha = 1;

//                 case "clouds":
//                     backDecoEffectObj = new FlxBackdrop(AssetPaths.cloudsBack__png, XY);
//                     backDecoEffectObj.velocity.set(-60, 0);
//                     backDecoEffectObj.alpha = 0.65;
//             }

//             if (backDecoEffectObj != null)
//             {
//                 backDecoEffectObj.scrollFactor.set(0, 0);
//                 currentBackEffectName = backDecoEffect;
//                 lastBackScrollBoost = backDecoScrollBoost;
//                 insert(1, backDecoEffectObj);
//             }
//         }
//     }
// }

// function bgEffect():Void
// {
//     var backEffectLayer = tiledData.getLayer("mapEffect");
//     var scrollBoost:Float = 0;
    
//     var scrollSettingsLayer = tiledData.getLayer("ScrollSettings");
//     if (scrollSettingsLayer != null && scrollSettingsLayer.type == TiledLayerType.OBJECT)
//     {
//         var castScroll:TiledObjectLayer = cast scrollSettingsLayer;
//         if (castScroll.properties.contains("speed"))
//             scrollBoost = Std.parseFloat(Std.string(castScroll.properties.get("speed")));
//     }

//     if (backEffectLayer != null && backEffectLayer.properties.contains("effectType"))
//     {
//         var newEffect = backEffectLayer.properties.get("effectType");

//         if (newEffect != currentBackEffectName || scrollBoost != lastBackScrollBoost)
//         {
//             if (backEffectObj != null) { remove(backEffectObj); backEffectObj.destroy(); backEffectObj = null; }

//             switch (newEffect)
//             {
//                 case "fog":
//                     backEffectObj = new FlxBackdrop(AssetPaths.white_fog__png, X);
//                     backEffectObj.velocity.set(scrollSettingsLayer == null ? 40 : (360 - scrollBoost), 0);
//                     backEffectObj.alpha = 0.25;

//                 case "clouds":
//                     backEffectObj = new FlxBackdrop(AssetPaths.cloudsBack__png, XY);
//                     backEffectObj.velocity.set(-60, 0);
//                     backEffectObj.alpha = 0.65;
//             }

//             if (backEffectObj != null)
//             {
//                 backEffectObj.scrollFactor.set(0, 0);
//                 currentBackEffectName = newEffect;
//                 lastBackScrollBoost = scrollBoost;
//                 insert(2, backEffectObj);
//             }
//         }
//     }
// }

// function bgEffectDouble():Void
// {
//     var doubleLayer = tiledData.getLayer("doubleEffect");
//     var dScrollBoost:Float = 0;
    
//     var scrollSettingsLayer = tiledData.getLayer("ScrollSettings");
//     if (scrollSettingsLayer != null && scrollSettingsLayer.type == TiledLayerType.OBJECT)
//     {
//         var castScroll:TiledObjectLayer = cast scrollSettingsLayer;
//         if (castScroll.properties.contains("speed"))
//             dScrollBoost = Std.parseFloat(Std.string(castScroll.properties.get("speed")));
//     }

//     if (doubleLayer != null && doubleLayer.properties.contains("doubleEffectType"))
//     {
//         var doubleEffect = doubleLayer.properties.get("doubleEffectType");

//         if (doubleEffect != currentDoubleEffectName || dScrollBoost != lastDoubleScrollBoost)
//         {
//             if (doubleEffectObj != null) { remove(doubleEffectObj); doubleEffectObj.destroy(); doubleEffect = null; }

//             switch (doubleEffect)
//             {
//                 case "fog":
//                     doubleEffectObj = new FlxBackdrop(AssetPaths.white_fog__png, X);
//                     doubleEffectObj.velocity.set(scrollSettingsLayer == null ? 40 : (360 - lastDoubleScrollBoost), 0);
//                     doubleEffectObj.alpha = 0.25;

//                 case "clouds":
//                     doubleEffectObj = new FlxBackdrop(AssetPaths.cloudsDouble__png, XY);
//                     doubleEffectObj.velocity.set(-20, 0);
//                     doubleEffectObj.alpha = 0.65;
//             }

//             if (doubleEffect != null)
//             {
//                 doubleEffectObj.scrollFactor.set(0, 0);
//                 currentDoubleEffectName = doubleEffect;
//                 lastDoubleScrollBoost = dScrollBoost;
//                 insert(3, doubleEffectObj);
//             }
//         }
//     }
// }

// function foregroundEffect():Void
// {
//     var frontLayer = tiledData.getLayer("foregroundEffect");
//     var frontScrollBoost:Float = 0;
    
//     var scrollSettingsLayer = tiledData.getLayer("ScrollSettings");
//     if (scrollSettingsLayer != null && scrollSettingsLayer.type == TiledLayerType.OBJECT)
//     {
//         var castScroll:TiledObjectLayer = cast scrollSettingsLayer;
//         if (castScroll.properties.contains("speed"))
//             frontScrollBoost = Std.parseFloat(Std.string(castScroll.properties.get("speed")));
//     }

//     if (frontLayer != null && frontLayer.properties.contains("foregroundEffectType"))
//     {
//         var frontTypeName = frontLayer.properties.get("foregroundEffectType");

//         if (frontTypeName != currentFrontEffectName || frontScrollBoost != lastFrontScrollBoost)
//         {
//             if (frontEffectObj != null) { remove(frontEffectObj); frontEffectObj.destroy(); frontEffectObj = null; }

//             switch (frontTypeName)
//             {
//                 case "speed":
//                     frontEffectObj = new FlxBackdrop(AssetPaths.speed_lines__png, X);
//                     var speedLayer = (scrollSettingsLayer == null) ? 40 : (frontScrollBoost - 3000);
//                     frontEffectObj.velocity.set(speedLayer, 0);
//                     frontEffectObj.alpha = 1;

//                 case "sandstorm":
//                     frontEffectObj = new FlxBackdrop(AssetPaths.sandstorm__png, XY);
//                     var speedLayer = (scrollSettingsLayer == null) ? 40 : (frontScrollBoost - 3000);
//                     frontEffectObj.velocity.set(speedLayer, 0);
//                     frontEffectObj.alpha = 1;
//             }

//             if (frontEffectObj != null)
//             {
//                 frontEffectObj.scrollFactor.set(0, 0);
//                 currentFrontEffectName = frontTypeName;
//                 lastFrontScrollBoost = frontScrollBoost;
//                 add(frontEffectObj);
//             }
//         }
//     }
// }

// function topEffect():Void
// {
//     var topLayer = tiledData.getLayer("topEffect");
//     var topScrollBoost:Float = 0;
    
//     var scrollSettingsLayer = tiledData.getLayer("ScrollSettings");
//     if (scrollSettingsLayer != null && scrollSettingsLayer.type == TiledLayerType.OBJECT)
//     {
//         var castScroll:TiledObjectLayer = cast scrollSettingsLayer;
//         if (castScroll.properties.contains("speed"))
//             topScrollBoost = Std.parseFloat(Std.string(castScroll.properties.get("speed")));
//     }

//     if (topLayer != null && topLayer.properties.contains("topEffectType"))
//     {
//         var topTypeName = topLayer.properties.get("topEffectType");

//         if (topTypeName != currentTopEffectName || topScrollBoost != lastTopScrollBoost)
//         {
//             if (topEffectObj != null) { remove(topEffectObj); topEffectObj.destroy(); topEffectObj = null; }

//             switch (topTypeName)
//             {
//                 case "speed":
//                     topEffectObj = new FlxBackdrop(AssetPaths.speed_lines__png, X);
//                     var speedLayer = (scrollSettingsLayer == null) ? 40 : (topScrollBoost - 3000);
//                     topEffectObj.velocity.set(speedLayer, 0);
//                     topEffectObj.alpha = 1;

//                 case "trees":
//                     topEffectObj = new FlxBackdrop(AssetPaths.trees__png, XY);
//                     var speedLayer = (scrollSettingsLayer == null) ? 80 : (topScrollBoost - 2000);
//                     topEffectObj.velocity.set(speedLayer, 0);
//                     topEffectObj.alpha = 1;
//             }

//             if (topEffectObj != null)
//             {
//                 topEffectObj.scrollFactor.set(0, 0);
//                 currentTopEffectName = topTypeName;
//                 lastTopScrollBoost = topScrollBoost;
//                 add(topEffectObj);
//             }
//         }
//     }
// }